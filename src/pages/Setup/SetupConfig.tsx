import React from 'react';

export const ProgressOrder = ['welcome', 'workspace', 'usageType', 'complete'];

const SetupConfig = () => {
  return (
    <div>SetupConfig</div>
  )
}

export default SetupConfig